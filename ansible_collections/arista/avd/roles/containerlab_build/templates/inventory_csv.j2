#jinja2:1strip_blocks: True
hostname,type,management_ip,platform,mac_address
{% for host in groups['AMS'] %}
{% set mac = hostvars[host].ansible_host.split('.') %}
{# {{inventory_hostname}},{{type}},{{ansible_host}},cEOS,0c:1d:c0:1d:ff:00 #}
{{host}},{{hostvars[host].type}},{{ hostvars[host].ansible_host}}/24,cEOS,0c:1d:c0:1d:{{mac[2]}}:{{mac[3]}}
{% endfor %}

